
Pomodoro
gaa
gcm git commit

commande d'un autre programme (hub) 
git creat nomgit/nomdossier

-------------------------------------
npm init -y 
- configure le package.json
create file .babelrc

nisd parcel-bundler
npm install -save -d parcel-bundler

--------------------------------------
cat ~/ .gitignore_global
nis react react-dom

------------------------------------

faire de la déstructuration
propTypes : vérifier en direct qu'on a le bon type de composant
npm install propTypes
PropsDrilling

useseffect : mécanisque de nettoyage d'effet de bord
attention quand le composant disparait, faut arrête le intervalId

à achaque fois qu'on rappel la fonction, ça affecte la mémoire,
utiliser useCallback avec comme 1er argument la fonction du callball et 2 argument un tableau

react portal,, injecter du code de composant dans un noeud du dom qui fait pas partie de l'app


